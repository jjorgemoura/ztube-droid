<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zindon.ztube"
    android:versionCode="1"
    android:versionName="@string/app_version" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="17" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" 
        android:name=".application.YTApplication">
        <activity
            android:name="com.zindon.ztube.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="PlaylistActivity" android:label="@string/app_name" android:parentActivityName="com.zindon.ztube.MainActivity" >
        	<!-- Parent activity meta-data to support 4.0 and lower -->
        	<meta-data
	            android:name="android.support.PARENT_ACTIVITY"
	            android:value="com.zindon.ztube.MainActivity" /> 
            </activity>

        <activity android:name="VideoListActivity" android:label="@string/app_name" android:parentActivityName="com.zindon.ztube.PlaylistActivity">
            <meta-data
	            android:name="android.support.PARENT_ACTIVITY"
	            android:value="com.zindon.ztube.PlaylistActivity" /> 
            </activity>
        <activity android:name="VideoDetailActivity" android:label="@string/app_name" android:parentActivityName="com.zindon.ztube.VideoListActivity">
            <meta-data
	            android:name="android.support.PARENT_ACTIVITY"
	            android:value="com.zindon.ztube.VideoListActivity" />             
        </activity>

                <activity android:name="SplashActivity" android:label="@string/app_name" android:screenOrientation="portrait" android:theme="@android:style/Theme.NoTitleBar.Fullscreen"></activity>
    </application>

</manifest>
